apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.microservice.name }}-secret
  namespace: {{ .Values.microservice.namespace }}
type: Opaque
stringData:  ## use stringData for plain text values, Kubernetes automatically encodes those values into Base64
  MONGODB_URI: {{ .Values.secrets.MONGODB_URI }}